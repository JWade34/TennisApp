<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Send Match Invite</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track': 'reload' %>
  <%= javascript_importmap_tags %>
</head>
<body class="bg-gray-100">
  <div class="container mx-auto p-4">

    <!-- Match Invite Form -->
    <div class="bg-white shadow-md rounded-lg mt-6 p-6">
      <h2 class="text-xl font-semibold mb-4">Send Match Invite to <%= @recipient.full_name %></h2>
      <%= form_with model: @match_invite, url: match_invites_path, method: :post do |f| %>
        <div class="mb-4">
          <%= f.hidden_field :recipient_id, value: @recipient.id %>
          <%= f.label :match_date, class: 'block text-gray-700' %>
          <%= f.date_field :match_date, class: 'mt-1 block w-full' %>
        </div>
        <%= f.submit "Send Invite", class: "bg-blue-500 text-white px-4 py-2 rounded-lg hover:bg-blue-600" %>
      <% end %>
    </div>

  </div>
</body>
</html>
